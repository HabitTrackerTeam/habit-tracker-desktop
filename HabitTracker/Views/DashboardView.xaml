<UserControl x:Class="HabitTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HabitTracker"
             xmlns:views="clr-namespace:HabitTracker.Views"
             xmlns:tabs="clr-namespace:HabitTracker.Views.Tabs">
    <Grid Visibility="{Binding IsDashboardVisible, Converter={StaticResource BoolToVis}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Fixed left sidebar -->
        <Border Grid.Column="0"
                BorderBrush="{DynamicResource InputBorderBrush}"
                BorderThickness="0,0,1,0"
                Padding="25,30,20,25">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,40">
                    <Border Width="42" Height="42" Background="#1B2A2F" CornerRadius="12" Margin="0,0,15,0">
                        <TextBlock Text="🌱" Foreground="#328A5D" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Habit Tracker" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextMainBrush}" />
                        <TextBlock Text="DAILY GROWTH" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button x:Name="NavDashboard" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToDashboard_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Dashboard, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="NavHabits" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToHabits_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding HabitsManager, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="NavCalendar" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToCalendar_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Calendar, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="NavStatistics" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToStatistics_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Statistics, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="NavMeasurements" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToMeasurements_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📏" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Measurements, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="NavSettings" Style="{StaticResource SidebarMenuItemStyle}" Click="SwitchToSettings_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Settings, Source={StaticResource Loc}}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <StackPanel Orientation="Horizontal" Margin="15,10,0,10" VerticalAlignment="Center" Cursor="Hand">
                        <Border Width="24" Height="24" CornerRadius="12" Margin="0,0,15,0">
                            <Border.Background>
                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill" />
                            </Border.Background>
                        </Border>
                        <TextBlock Text="{Binding Nickname}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" />
                    </StackPanel>

                    <Button Style="{StaticResource SidebarMenuItemStyle}" Click="LogoutButton_Click" Padding="15,10" Margin="0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🚪" FontSize="16" Margin="0,0,15,0" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Logout, Source={StaticResource Loc}}" FontSize="13" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Column="1" x:Name="MainContentArea" Margin="40,20,20,20">
            <tabs:HomeTabView Visibility="{Binding IsDashboardContent, Converter={StaticResource BoolToVis}}" />
            <tabs:HabitManagerTabView Visibility="{Binding IsHabitsVisible, Converter={StaticResource BoolToVis}}" />
            <tabs:MeasurementsView x:Name="MeasurementsViewControl" Visibility="{Binding DataContext.IsMeasurementsVisible, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BoolToVis}}" />
            <tabs:CalendarTabView Visibility="{Binding IsCalendarVisible, Converter={StaticResource BoolToVis}}" />
            <tabs:StatisticsTabView Visibility="{Binding IsStatisticsVisible, Converter={StaticResource BoolToVis}}" />
            <tabs:SettingsTabView Visibility="{Binding DataContext.IsSettingsVisible, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BoolToVis}}" />
        </Grid>
    </Grid>
</UserControl>
